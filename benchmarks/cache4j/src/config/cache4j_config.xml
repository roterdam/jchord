<?xml version="1.0" encoding="Windows-1251"?>

<!--
 |
 | clean-interval - интервал очистки кеша (по умолчанию 30секунд)
 | можно указывать: миллисекунды, +s - секунды, +m - минуты, +h - часы
 | пример: 60s или 1m или 1h
 |
 -->

<cache-factory clean-interval="10s">

    <!--
    |
    | Тег:
    | cache - кеш
    |
    | Атрибуты:
    | id - идентификатор кеша, должен быть уникальнм
    | desc - описание экземпляра кеша
    |
    | ttl - максимальное время жизни объекта в кеше (Time To Live)
    | можно указывать: миллисекунды, +s - секунды, +m - минуты, +h - часы
    | по умолчанию: 0 - без ограничений
    |
    | idle - максимальное время бездействия объекта в кеше (Idle Time)
    | можно указывать: миллисекунды, +s - секунды, +m - минуты, +h - часы
    | по умолчанию: 0 - без ограничений
    |
    | max-memory-size - максимальный объём памяти занимаемый всеми объектами
    | кеша(байт).
    | можно указывать: байт, +к - килобайт, +m - мегабайт
    | по умолчанию: 0 - без ограничений
    |
    | max-size - максимальное количество объектов в кеше
    | по умолчанию: 0 - без ограничений
    |
    | type - тип кеша:
    |        blocking
    |        synchronized (по умолчанию)
    |        nocache
    |
    | algorithm - алгоритм удаления объектов из кеша:
    |        lru (Least Recently Used) (по умолчанию)
    |        lfu (Least Frequently Used)
    |        fifo (First In First Out)
    |
    |  reference - тип ссылки на объект содержащийся в кеше:
    |         strong сильная ссылка (по умолчанию)
    |         soft   магкая ссылка (SoftReference)
    -->

    <!--
     | Пример 1: максимальное время жизни         : 1 час
     |           максимальное время бездействия   : 30 минут
     |           максимальное количество объектов : 10000
     |           максимальный объем памяти        : 50 мегабайт
     -->
    <cache id="cache_id1" desc="example cache 1"
           ttl="1h"
           idle="30m"
           max-size="10000"
           max-memory-size="50m"/>

    <!--
     | Пример 2: максимальное время жизни         : 30 минут
     |           максимальное время бездействия   : 20 минут
     |           максимальное количество объектов : 1000
     |           максимальный объем памяти        : 30 мегабайт
     |           тип кеша                         : кеш с блокированием на уровне объектов
     |           алгоритм вытеснения объектов     : LFU
     |           тип ссылки на кешируемые объекты : мягкая ссылка
     -->
    <cache id="cache_id2" desc="example cache 2"
           ttl="30m"
           idle="20m"
           max-size="1000"
           max-memory-size="30m"
           type="blocking"
           algorithm="lfu"
           reference="soft"/>

</cache-factory>
